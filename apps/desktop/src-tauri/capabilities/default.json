{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop shell capability set for bootstrap runtime events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
