# Validation Standard

- `pnpm validate` is the root repository integrity check.
- Validation must cover structure, workspace wiring, docs, and fixture shapes.
- New commands and package boundaries must be reflected in docs.
- Validation may be standards-first at bootstrap, but runnable commands are mandatory.
