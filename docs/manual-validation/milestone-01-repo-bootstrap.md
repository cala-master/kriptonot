# Milestone 01: Repo Bootstrap

## Commands

```bash
pnpm validate
```

## Manual Checks

- verify root manifests exist
- verify workspace config points at `apps/*` and `packages/*`
- verify Python starter files are not referenced as the active stack in README
