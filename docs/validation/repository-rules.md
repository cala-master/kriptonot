# Repository Rules

- Root commands must remain `pnpm validate`, `pnpm test`, `pnpm test:contracts`, `pnpm test:integration`, and `pnpm manual:bootstrap`.
- The root workspace must include `apps/*` and `packages/*`.
- Required docs from the bootstrap spec must exist.
- Package names must stay aligned with their directory names.
